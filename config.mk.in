CC?=__CC__
LD?=__LD__
AR?=__AR__

CFLAGS?=__CFLAGS__
LDFLAGS?=__LDFLAGS__

BASEDIR?=__BASEDIR__
PREFIX?=__PREFIX__
DESTDIR?=$(PREFIX)

.DEFAULT_GOAL=all

clean:
	find . -name "*.o" -delete
	find . -name "*.so" -delete
	find . -name "*.a" -delete

distclean: clean
	rm Makefile
	rm config.mk
